{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --exec ts-node src/index.ts",
    "start": "ts-node src/index.ts",
    "build": "tsc",
    "migrate": "ts-node src/migrateSchema.ts",
    "init-data": "ts-node src/initAiData.ts",
    "test-schema": "ts-node src/testSchema.ts",
    "test-templates": "ts-node src/testTemplateApi.ts",
    "db-reset": "npm run migrate && npm run init-data",
    "setup": "npm install && npm run db-reset",
    "clean": "rm -rf dist node_modules",
    "type-check": "tsc --noEmit",
    "check": "npm run type-check && npm run test-schema",
    "api-docs": "echo 'API 문서: http://localhost:4000/api-docs'",
    "health": "curl http://localhost:4000/ping || echo '서버가 실행되지 않았습니다'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/dotenv": "^6.1.1",
    "@types/express": "^5.0.3",
    "@types/node": "^24.0.2",
    "@types/pg": "^8.15.4",
    "@types/swagger-jsdoc": "^6.0.4",
    "@types/swagger-ui-express": "^4.1.6",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "pg": "^8.16.0",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.0"
  }
}
